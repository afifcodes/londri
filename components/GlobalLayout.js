export default function GlobalLayout({ children }) {
  return (
    <div className="font-cool font-semibold text-base sm:text-xl bg-gradient-to-br from-white to-blue-300">
      {children}
    </div>
  );
}
